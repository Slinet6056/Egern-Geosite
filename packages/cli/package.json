{
  "name": "@egern-geosite/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "egern-geosite": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @egern-geosite/core build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @egern-geosite/core build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @egern-geosite/core build && vitest run"
  },
  "dependencies": {
    "@egern-geosite/core": "workspace:*"
  }
}
